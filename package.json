{
  "dependencies": {
    "iitcpluginkit": "^1.9.6"
  },
  "license": "Unlicense",
  "type": "module",
  "scripts": {
    "build": "yarn build:all",
    "build:dev": "yarn ipk build:dev",
    "build:prod": "yarn ipk build:prod",
    "build:changelog": "node scripts/create-changelog.js",
    "build:themes": "node scripts/create-themes-file.js",
    "build:all": "yarn build:themes && yarn build:changelog && yarn ipk build:prod",
    "build:alldev": "yarn build:themes && yarn build:changelog && yarn ipk build:dev",
    "start": "yarn ipk fileserver",
    "autobuild": "yarn ipk autobuild"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "eslint-plugin-prefer-arrow-functions": "^3.9.1",
    "eslint-plugin-unicorn": "^62.0.0",
    "typescript-eslint": "^8.51.0"
  }
}
